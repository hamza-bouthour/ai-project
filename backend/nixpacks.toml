[phases.setup]
nixPkgs = [
  "nodejs_20",
  "python311"
]

[phases.install]
cmds = [
  "npm install",
  "rm -rf .venv",
  "python3 -m venv .venv",
  ".venv/bin/pip install -U pip setuptools wheel",
  ".venv/bin/pip install numpy==1.24.3 pandas==2.1.4 scikit-learn==1.3.2 joblib==1.3.2"
]

[phases.build]
cmds = [
  ".venv/bin/python ml/train.py"
]

[start]
cmd = "node server.js"
